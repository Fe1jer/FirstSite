<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/css/bootstrap.min.css" type="text/css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/StyleSheet.css" type="text/css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/Cookie.css" type="text/css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/headers.css" type="text/css" asp-append-version="true" />
    <meta charset="utf-8">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">Интернет магазин</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav me-auto mb-2 mb-md-0">
                        <li class="nav-item active">
                            <a class="nav-link" aria-current="page" asp-controller="Home" asp-action="Catalog">Главная</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" asp-controller="Products" asp-action="Index">Все товары</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" asp-controller="ShopCart" asp-action="Index">Корзина</a>
                        </li>
                        @if (User.IsInRole("admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" asp-controller="Roles" asp-action="Index">Управление ролями</a>
                            </li>
                        }
                        @if (User.IsInRole("moderator"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" asp-controller="Courier" asp-action="Index">Все заказы</a>
                            </li>
                        }
                        @if (User.IsInRole("courier"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" asp-controller="Courier" asp-action="CourierOrders">Мои заказы</a>
                            </li>
                        }
                    </ul>
                    <form asp-controller="Products" asp-action="Search" class="d-flex">
                        <input class="form-control me-2" type="search" placeholder="Поиск" aria-label="Search">
                        <button class="btn btn-outline-success" type="submit">Поиск</button>
                    </form>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <div class="navbar-nav">
                            <div class="nav-link">
                                <a href="#" class="d-block link-light text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                    <img src="https://github.com/mdo.png" alt="mdo" width="32" height="32" class="rounded-circle">
                                </a>
                                <ul class="dropdown-menu shadow" aria-labelledby="dropdownUser1" style="">
                                    <li class="nav-item"><a class="nav-link text-dark" asp-controller="Account" asp-action="Profile">Profile</a></li>
                                    <li class="nav-item"><a class="nav-link text-dark" asp-controller="Account" asp-action="Settings">Settings</a></li>
                                    <li class="nav-item"><hr class="dropdown-divider"></li>
                                    <li class="nav-item"> <a class="nav-link text-dark" aria-current="page" asp-controller="Account" asp-action="Logout">Выйти</a></li>
                                </ul>
                            </div>
                        </div>
                    }
                    else
                    {
                        <ul class="navbar-nav">
                            <li class="nav-item active">
                                <a class="nav-link" aria-current="page" asp-controller="Account" asp-action="Login">Войти</a>
                            </li>
                        </ul>
                    }
                </div>
            </div>
        </nav>

    </header>
    <div class="container">
        @RenderBody()
    </div>
    <div id="cookie_notification">
        <p class="text-dark abs">Для улучшения работы сайта и его взаимодействия с пользователями мы используем файлы cookie. Продолжая работу с сайтом, Вы разрешаете использование cookie-файлов. Вы всегда можете отключить файлы cookie в настройках Вашего браузера.</p>
        <button class="btn btn-info cookie_accept">Принять</button>
    </div>
    <script src="~/js/bootstrap.bundle.min.js" asp-append-version="true"></script>
    <script src="~/js/Cookie.js" asp-append-version="true"></script>
</body>
</html>
